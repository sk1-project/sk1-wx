# Testing requirements for sK1/UniConvertor
# Compatible with Python 2.7 and Python 3.x

# pytest - last version supporting Python 2.7 is 4.6.x
pytest>=3.0,<5.0; python_version < '3.0'
pytest>=6.0; python_version >= '3.0'

# For GUI testing with virtual X server (Python 3+ only, not compatible with Python 2.7)
# Note: For Python 2.7, skip GUI tests with: pytest -m "not gui"
pytest-xvfb>=1.2.0; python_version >= '3.0'
